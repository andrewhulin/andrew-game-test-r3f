# CLAUDE.md

## Project Overview

This is a React Three Fiber (R3F) game project. The repository is currently in its initial setup phase.

- **Repository:** andrew-game-test-r3f
- **Primary Branch:** main

## Project Status

The project is in early development. No application code, dependencies, or build configuration have been set up yet.

## Planned Tech Stack (based on project naming)

- React
- React Three Fiber (R3F) for 3D rendering
- TypeScript (recommended)

## Commands

No build system or scripts are configured yet. Once `package.json` is created, document commands here:

- `npm install` - Install dependencies
- `npm run dev` - Start development server
- `npm run build` - Build for production
- `npm test` - Run tests

## Project Structure

```
/
├── Sample file.md
└── CLAUDE.md
```

## Code Conventions

- Not yet established. Define conventions here as the project develops.
